---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: hellodois
  namespace: numero-dois
  annotations:
    fluxcd.io/automated: "true"
    filter.fluxcd.io/hellodois: glob:v[!12]
spec:
  releaseName: api
  chart:
    git: git@github.com:lucas-stellet/noah-api
    path: charts/api
    ref: master
  values:
    api:
      image:
        repository: lustepe/simple-server-nodejs
        tag: v1
      containers:
        envFrom:
          configMap:
            data:
              MY_NAME: Noah