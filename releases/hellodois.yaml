---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: hellodois
  namespace: numero-dois
  annotations:
    fluxcd.io/automated: "true"
    filter.fluxcd.io/hellodois: glob:v[!2]
spec:
  releaseName: api
  chart:
    git: git@github.com:lucas-stellet/noah-api
    path: charts/api
    ref: master
  values:
    api:
      image:
        repository: lustepe/simple-server-nodejs
        tag: v2
      containers:
        envFrom:
          configMap:
            data:
              MY_NAME: Noah
